// API configuration
export const API_BASE_URL = 'https://official-joke-api.appspot.com';

// Routes
export const ROUTES = {
  HOME: '/',
  RESULTS: '/results-suspense', // Only use suspense version
} as const;
